import { ground_tiles } from 'tiles/tiles.constants'

const port = Number(process.env.PORT || 2222)

console.log(`\n👂 Backend server Listening on http://localhost:${port}`)

console.log(ground_tiles)
