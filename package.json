{
  "name": "later",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"yarn server\" \"yarn client\"",
    "client": "cross-env NODE_ENV=development parcel src/client/index.html --no-autoinstall --no-hmr",
    "client:reload": "cross-env NODE_ENV=development parcel src/client/index.html --no-autoinstall",
    "client:no-cache": "cross-env NODE_ENV=development parcel src/client/index.html --no-autoinstall --no-hmr --no-cache",
    "client:build": "rimraf client_build && cross-env NODE_ENV=production parcel build src/client/index.html --no-source-maps --out-dir client_build",
    "server": "cross-env NODE_ENV=development ts-node-dev --inspect=9228 --no-notify --respawn src/server/index.ts",
    "server:build": "rimraf server_build && NODE_ENV=production echo 'Not ready yet ;)'",
    "test": "cross-env NODE_ENV=development jest",
    "test:watch": "cross-env NODE_ENV=development jest --watch",
    "test:coverage": "cross-env NODE_ENV=development jest --coverage",
    "test:coverage:all": "cross-env NODE_ENV=development COVERAGE=all jest --coverage",
    "test:report": "http-server -s -o -c-1 -p 8888 coverage/lcov-report"
  },
  "dependencies": {
    "@colyseus/monitor": "^0.1.7",
    "colyseus": "^0.9.9",
    "colyseus.js": "^0.9.5",
    "express": "^4.16.3",
    "matter-js": "^0.14.1",
    "mousetrap": "^1.6.1",
    "pixi.js": "^5.0.0-alpha.2",
    "ramda": "^0.25.0",
    "uws": "^10.148.0"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/jest": "^22.2.3",
    "@types/matter-js": "^0.10.0",
    "@types/mousetrap": "^1.6.0",
    "@types/node": "^10.1.2",
    "@types/ramda": "^0.25.29",
    "@types/uws": "^0.13.2",
    "babel-core": "^6.26.3",
    "babel-plugin-module-resolver": "^3.1.1",
    "babel-plugin-ramda": "^1.6.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "babel-runtime": "^6.26.0",
    "concurrently": "^3.5.1",
    "cross-env": "^5.1.6",
    "http-server": "^0.11.1",
    "jest": "^22.4.4",
    "node-sass": "^4.9.0",
    "parcel-bundler": "^1.8.1",
    "rimraf": "^2.6.2",
    "standard": "^11.0.1",
    "ts-jest": "^22.4.6",
    "ts-node": "^6.0.5",
    "ts-node-dev": "^1.0.0-pre.24",
    "typescript": "^2.8.3"
  },
  "engines": {
    "node": ">= 10.2.1",
    "npm": ">= 5.6.0",
    "yarn": ">= 1.7.0"
  }
}
